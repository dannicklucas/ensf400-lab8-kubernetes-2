apiVersion: v1
kind: Service
metadata:
  name: app-2-svc
spec:
  selector:
    app: app-2-dep   # This selector should match the labels of the pods from the Deployment
  ports:
    - port: 80   # Port to expose on the service
      targetPort: 80